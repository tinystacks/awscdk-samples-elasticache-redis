import { NameRestriction } from '.';
interface NameValidationResponse {
    isValid: boolean;
    reason?: string;
}
declare class NameValidationResponse implements NameValidationResponse {
    constructor(isValid: boolean, reason?: string);
}
declare function validateName(name?: string, nameRestriction?: NameRestriction, label?: string): NameValidationResponse;
export { NameValidationResponse, validateName };
