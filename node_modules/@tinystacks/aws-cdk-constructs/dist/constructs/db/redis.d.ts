import { Construct } from 'constructs';
import * as ec2 from 'aws-cdk-lib/aws-ec2';
export interface RedisCacheProps {
    vpc: ec2.IVpc;
    subnets: ec2.ISubnet[];
    securityGroupsList: ec2.ISecurityGroup[];
    instanceType?: string;
    dbIdentifier: string;
    primaryVpcCidrBlock?: string;
}
export declare class Redis extends Construct {
    private readonly vpc;
    private readonly id;
    private readonly securityGroupsList;
    private readonly primaryVpcCidrBlock;
    private readonly subnets;
    private readonly dbIdentifier;
    private readonly instanceType;
    private replicationGroup;
    private elasticacheSecret;
    constructor(scope: Construct, id: string, props: RedisCacheProps);
    initRedisCache(): void;
    get redisEndpoint(): string;
    get redisPort(): string;
    get redisAuthTokenSecretArn(): string;
}
