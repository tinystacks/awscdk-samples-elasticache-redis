import { Construct } from 'constructs';
import * as s3Deployment from 'aws-cdk-lib/aws-s3-deployment';
import { BucketProps } from 'aws-cdk-lib/aws-s3';
interface S3Props {
    bucketProps?: BucketProps;
    existingBucketArn?: string;
}
export declare class S3 extends Construct {
    private readonly bucket;
    private readonly id;
    private readonly bucketProps;
    constructor(scope: Construct, id: string, props: S3Props);
    private createBucket;
    uploadSource(source: [s3Deployment.ISource], bucketKeyPrefix: string): void;
    get bucketArn(): string;
    get bucketName(): string;
}
export {};
