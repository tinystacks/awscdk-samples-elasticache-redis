import { Construct } from 'constructs';
export interface EksCleanupProps {
    vpcId: string;
    clusterName: string;
}
export declare class EksCleanup extends Construct {
    readonly response: any;
    constructor(scope: Construct, id: string, props: EksCleanupProps);
}
