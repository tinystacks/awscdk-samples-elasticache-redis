import { Casing, NameRestriction } from './types';
declare function generateName(args: {
    identifiers: string[];
    separator?: string;
    nameRestriction?: NameRestriction;
    casing?: Casing;
}): string;
export { generateName };
